import { default as PsdToPhaserPlugin } from '../../../PsdToPhaser';
import { ZoneLayer } from '../../../types';
export declare function placeZones(scene: Phaser.Scene, layer: ZoneLayer, plugin: PsdToPhaserPlugin, group: Phaser.GameObjects.Group, resolve: () => void, _psdKey: string): void;
