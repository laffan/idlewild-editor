import { default as PsdToPhaserPlugin } from '../../../PsdToPhaser';
import { PointLayer } from '../../../types';
export declare function placePoints(scene: Phaser.Scene, layer: PointLayer, plugin: PsdToPhaserPlugin, group: Phaser.GameObjects.Group, resolve: () => void, _psdKey: string): void;
