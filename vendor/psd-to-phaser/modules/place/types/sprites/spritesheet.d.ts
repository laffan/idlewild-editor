import { default as PsdToPhaserPlugin } from '../../../../PsdToPhaser';
import { SpritesheetLayer } from '../../../../types';
export declare function placeSpritesheet(scene: Phaser.Scene, layer: SpritesheetLayer, plugin: PsdToPhaserPlugin, _psdKey: string, textureKey?: string): Phaser.GameObjects.Group;
